# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

This project is pinned to **SDK 56**, not the latest SDK. Do not upgrade it without being asked —
SDK 57 was tried and rolled back because the installed Expo Go build did not support it.
